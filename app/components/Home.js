var React = require('react');

var Home = React.createClass({
    render: function() {
        return (
            <h2 className="text-center">
                Some subhead child component
            </h2>
        )
    }
});

module.exports = Home;
